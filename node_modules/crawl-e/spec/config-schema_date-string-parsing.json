{
  "id": "http://crawl-e.cinepass.de/schemas/config-schema_date-string-parsing.json",
  "$schema": "http://json-schema.org/draft-04/schema",
  "properties": {
    "dateFormat": {
      "type": ["string", "array"],
      "items": {
        "type": "string"
      },
      "description": "Format(s) for parsing a date string selected by the `date` or `dates` ValueGrabber"
    },
    "dateLocale": {
      "type": "string",
      "description": "Locale for parsing a date string. (Specified as [ISO 639 code](http://www.localeplanet.com/icu/iso639.html))"
    },
    "preserveYear": {
      "type": "boolean",
      "description": "Disables year correction of date without explicit year information.",
      "default": false
    }        
  }
}